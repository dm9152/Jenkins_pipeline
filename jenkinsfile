pipeline {

    agent any

    matrix {
    axes {
        axis {
            name 'PLATFORM'
            values 'linux', 'mac', 'windows'
        }
    }
    stages {
        stage('build') {
            steps {
                echo "construire sur la plate-forme : ${PLATFORM}"
            }
        }
        stage('test') {
            steps {
                echo "tester sur la plate-forme : ${PLATFORM}"
            }
        }
        stage('deploy') {
            steps {
                echo "déployer sur la plate-forme : ${PLATFORM}"
            }
        }
    }
    }

}